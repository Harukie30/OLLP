# Copy to `.env.local` in the project root (gitignored).

# SQLite — run `pnpm db:migrate` after setting this
DATABASE_URL="file:./dev.db"

# Staff dashboard at /staff (footer link hidden until both vars are set)
STAFF_EVENTS_PASSWORD=
